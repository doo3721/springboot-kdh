info:
  name: 마이페이지
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/members/me"
  auth:
    type: basic
    username: user1@example.com
    password: "1234"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
