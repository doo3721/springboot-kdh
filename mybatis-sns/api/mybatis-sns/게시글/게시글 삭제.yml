info:
  name: 게시글 삭제
  type: http
  seq: 5

http:
  method: DELETE
  url: localhost:8080/api/v1/posts/:id
  headers:
    - name: X-Member-Id
      value: "2"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
