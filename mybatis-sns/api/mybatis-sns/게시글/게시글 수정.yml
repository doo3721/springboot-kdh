info:
  name: 게시글 수정
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/api/v1/posts/:id
  headers:
    - name: X-Member-Id
      value: "1"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "content": "REST API UPDATE",
        "imageUrl": "https://example.com/img/spring.png"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
